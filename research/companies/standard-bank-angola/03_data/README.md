# 03_data — dados transformados

Só depois da extração. Cada linha segue a cadeia:

```
original → normalizado → métrica → período → fonte → página
```

É a base da futura base de dados da Kuzela (`data/input/` no plano, §2). Os conceitos e os mapeamentos são decididos pelo Uziel; o Claude propõe e assinala as dúvidas.
